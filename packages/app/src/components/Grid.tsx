import React from "react";

type CustomGridProps = {
    readonly value: unknown[];
    readonly count: number;
    readonly page: number;
    readonly pageSize: number;
}

export const CustomGrid = (props: CustomGridProps) => <div></div>